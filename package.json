{
  "name": "freshdesk-api-client",
  "version": "1.0.0",
  "description": "node freshdesk-api-client",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "start": "nodemon ./dist/index.js",
    "prod": "npm run build && npm run start",
    "watch": "nodemon",
    "test": "mocha --timeout 20000 -r ts-node/register",
    "test-all": "mocha --timeout 5000 -r ts-node/register tests/**/*.spec.ts"
  },
  "keywords": [
    "node",
    "npm",
    "freshdesk",
    "freshdesk-api-client"
  ],
  "author": "Niyaz K",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "form-data": "^2.3.3"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^12.0.2",
    "chai": "^4.2.0",
    "config": "^3.1.0",
    "mocha": "^6.1.4",
    "nodemon": "^1.19.0",
    "ts-node": "^8.1.1",
    "typescript": "^3.4.5"
  }
}